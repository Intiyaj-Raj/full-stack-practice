# TODO - counter-app-using-context styling

- [x] Update `src/index.css` with global styles (centering, background, fonts).
- [x] Update `src/App.css` with layout/card + heading + button styles.
- [x] Update `src/App.jsx` to import `./App.css` and add wrapper/title classNames.
- [x] Update `src/components/Counter.jsx` to add classNames to containers/buttons.
- [ ] (After changes) run the dev server and visually verify styling + counter functionality.
